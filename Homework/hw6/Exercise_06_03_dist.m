% Clear workspace, command window, and close all figures
clc; clear; close all;

%%%%%%%%%%%%%%%%% TODO %%%%%%%%%%%%%%%%%
% matrix sizes and rank
M = ;
N = ;
r = ;

% compute the four matrices
A = randn(M, r) * randn(r, N);
At = % express A transpose using variable A
AtA = % express A transpose using variable A
AAt = % express A transpose using variable A

% calculate their ranks (A, At, AtA, AAt)
% print their ranks



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%